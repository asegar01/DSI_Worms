<Page
    x:Class="DSI_Worms.Settings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DSI_Worms"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="/Assets/#ABeeZee-Regular"/>
        </Style>

        <Style TargetType="ToggleButton">
            <Setter Property="Background" Value="#C9F1FD"/>
            <Setter Property="FontFamily" Value="/Fonts/#ABeeZee-Regular"/>
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="Width" Value="220"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="CornerRadius" Value="5, 5, 0, 0"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.Background>
            <ImageBrush Stretch="Fill" ImageSource="Assets/Stripes.jpg" Opacity="0.4"/>
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="150"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Image Source="Assets/Back.png" 
               Width="80" 
               HorizontalAlignment="Left" 
               VerticalAlignment="Top"
               Margin="20"
               Tapped="On_Back"/>

        <StackPanel Orientation="Vertical">
            <TextBlock Margin="0, 80, 0, 0"
                       Text="CONFIGURACIÓN"
                       HorizontalAlignment="Center"
                       FontSize="40"
                       FontWeight="SemiBold"
                       Foreground="#00A4D3"/>

            <Rectangle Width="700" 
                       Height="3"
                       Margin="0, 10, 0, 0"
                       Fill="#00A4D3"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <StackPanel Orientation="Vertical"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            Margin="0, 70, 0, 0">
                    <ToggleButton x:Name="General" Content="General" IsChecked="True"/>
                    <ToggleButton x:Name="Interface" Content="Interfaz" IsChecked="False"/>
                    <ToggleButton x:Name="Video" Content="Vídeo" IsChecked="False"/>
                    <ToggleButton x:Name="Audio" Content="Audio" IsChecked="False"/>
                </StackPanel>

                <Grid x:Name="Configuration">
                    <Rectangle Fill="White" Height="450" Width="880" StrokeThickness="1" Stroke="Gray"/>

                    <StackPanel Visibility="Visible">

                    </StackPanel>
                </Grid>
            </StackPanel>
        </Grid>
        
    </Grid>
    
</Page>
